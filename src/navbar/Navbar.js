import {BrowserRouter,Routes, Route} from 'react-router-dom'

const Navbar = () => {
    return(
        <BrowserRouter>
            <Routes>
                <Route/>
            </Routes>
        </BrowserRouter>
    )
}